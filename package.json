{
  "name": "oj-client",
  "version": "1.0.0",
  "description": "Online Judge Client Application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^27.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
